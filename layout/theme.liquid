<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <title>{{ shop.name }}</title>
    {{ content_for_header }}

    {{ 'style.css' | asset_url | stylesheet_tag }}


    {{ 'style.css' | asset_url | stylesheet_tag }}

  </head>
  <body>
    <main class="main">
      {{ content_for_layout }}
    </main>
  </body>
</html>