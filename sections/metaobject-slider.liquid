{% assign product = section.settings.product %}
{% assign product = all_product %}

<h1>{{ product.title }}</h1>

{% assign products_relateds = product.metafields.custom.produtos_relacionados.value %}


{{ products_relateds.value | json }}


{% schema %}
  {
    "name": "metaobject-slider",
    "settings": [
      {
        "type": "product",
        "id": "produto-principal",
        "label": "Produto Principal"
      }
    ],
    "presets": [
      {
        "name": "metaobject-slider"
      }
    ]
  }
{% endschema %}